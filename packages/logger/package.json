{
  "name": "@your-org/logger",
  "version": "0.1.0",
  "private": true,
  "description": "A reusable logger package for the Turborepo project",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w",
    "lint": "eslint . --ext .ts,.tsx",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "pino": "^8.0.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "eslint": "^8",
    "@your-org/eslint-config": "workspace:*",
    "@your-org/typescript-config": "workspace:*",
    "typescript": "^5"
  },
  "publishConfig": {
    "access": "public"
  }
}
